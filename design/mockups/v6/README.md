# V6 — Global Workspace Radar（用户自绘定稿，2026-06-10 批准）

- `global_workspace_radar.html`：用户提供的 V6 原稿（单文件 HTML/CSS/JS，可直接浏览器打开）。
  由 markdown 包裹件机械重建：剥除 code fence + 修复 `<`/`</` 跨块断裂；重建后
  结构检查 8/8 + node --check JS 语法通过。原件保存于用户 Documents。
- `DESIGN_SPEC_V6.md`：随稿设计规范原文（防设计漂移条款，原样入库）。

**法律地位**：本稿是 P1 UI 轨的批准设计北极星（取代被否决的 r1/ 三张）。与仓库既有
设计法律的逐条对账与立法调和见 [`../../V6_RECONCILIATION.md`](../../V6_RECONCILIATION.md)。

**数据免责**：稿内所有项目名（omega/noosphere…）、agent 名、模型名、数字全部是
illustration example，不是真实数据；实现绑定 daemon 真实事件流，不得复刻示例数据。
