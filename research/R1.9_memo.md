# R1.9 — turingosv4 移植前质量审计 memo（R-memo）

> 审计日期：2026-06-11 · 执行：R1.9 质量审计官（Claude，本地 session）
> 技术法律：`research/R1.9_audit_runbook.md`（§2-§6 逐段执行）
> **审计对象：turingosv4 @ `1f00012d5326d08f0e6ffe05f6ecec6a391c4896`（main，2026-05-29）**
> 审计方法：本地 `git clone --no-hardlinks` 至 `~/work/.audit-scratch/turingosv4-main` 后检出 main，在克隆内只读审计；**原仓 `~/work/turingosv4` 全程零写入**（R1 成立）。
> 原始证据：`audit_data/machine_sweep/**`、`audit_data/mutants/**`、`audit_data/critics/**`（本 memo 每个数字可溯源至其中的命令+输出文件）。

---

## 0. 头条（按 R6 惊喜协议升格；含前置 #2 要求的失败原文）

### 0.1 锚点裁决经过（移植锚点的前提事实）

开工前置第 1 步失败：原仓工作树**不干净**——163 个已跟踪文件被修改（+6471/−2198 行，未提交至任何分支），774 个未跟踪文件（768 个在 `handover/`），且 HEAD 在 `claude/p1-realvalue`（领先 main **143 commits**；快照分支 `claude/p1-realvalue-audit-snapshot-20260605` 与 HEAD 同 rev，**未捕获**该脏 diff）。已按"停下原文上报"上报，用户停机点裁决：**锚定 main `1f00012d`**。
推论（不可回避）：**143 个未合并 commits + 6.5k 行未提交工作不在本审计与移植范围内**，其去向是 §⑦ 参数 2 的核心输入。

### 0.2 基线验证结果：红（失败原文）

一键入口 = AGENTS.md §7/§14 收工三件套。结果（`audit_data/machine_sweep/baseline_run.log`）：

```
BASELINE_RESULT step1=101 step2=2 step3=0
step1 = cargo test --workspace --no-fail-fast   → 红（4 个测试失败）
step2 = bash scripts/run_constitution_gates.sh  → 红（工具性）
step3 = cargo test --test constitution_matrix_drift → 绿
```

**step1 的 4 个红，逐一归因（红必先归因纪律）：**

| # | 测试 | 失败原文（节选） | 归因 | 性质 |
|---|---|---|---|---|
| R-1a | `boot::tests::verify_trust_root_passes_on_intact_repo` | `Tampered { path: ".claude/hooks/judge.sh", expected: "a8d4a38f…", actual: "9c54ba5c…" }` | **平台无关语义红**：`git show 1f00012d:.claude/hooks/judge.sh` 的 sha256 = `9c54ba5c…`，与 `genesis_payload.toml:141` 的 pin（2026-05-09 R-020 retirement 重哈希）不符。judge.sh 被 commit `92c6ffe6`（2026-05-29 "fix: narrow judge.sh command-name matching"）修改后**未重哈希 pin**。原仓脏树同为 9c54（非脏改动所致，main 自身红） | 语义红 |
| R-1b | `fc3_n34_readonly_guard_verify_trust_root_intact_repo`（tests/fc_alignment_conformance.rs:173） | 同上 Tampered | 同 R-1a | 语义红 |
| R-1c | （连带）`turingosv4` 启动二进制 | `TRUST_ROOT_TAMPERED: …judge.sh hash mismatch` panic at src/main.rs:15 | **boot 二进制在 main 锚点上启动即死**（实跑 target/debug/deps/turingosv4-65b6… 验证） | 语义红 |
| R-2 | `boot::tests::verify_trust_root_detects_tamper_in_tempdir` | `assertion failed: path.ends_with("only.txt")` at src/boot.rs:478 | **并行执行污染型 flake**：单测直跑 PASS（`turingosv4-26af16cc… --exact` 实证），全套并行跑 FAIL。根因 UNVERIFIED | flake |
| R-3 | `sandbox_cwd_is_explicit_workspace`（tests/sanitized_runner.rs:68） | `left: "/private/var/folders/…" right: "/var/folders/…"` | **macOS symlink 规范化差异**（/var→/private/var）。Linux CI 不会红；但**移植目标就是 macOS**，故为真实移植工作项 | 平台红 |

**为什么 CI 没拦住 R-1**：全仓 3 个 workflow（constitution_gates.yml / co1_13_r022_ci.yml / validate-agent-pr.yml）中**没有任何一个跑 `cargo test --workspace`**——lib 单元测试与非宪法集成测试零 CI 覆盖。"测试全过"的真实含义是"164 门禁套件在 ubuntu CI 全过"，workspace 全套自 2026-05-29 起在干净 main 检出上即红。

**step2 失败原文（工具性）**：`grep: invalid option -- P`——跑器 `scripts/run_constitution_gates.sh:18` 用 GNU `grep -oP`，macOS BSD grep 不支持。在**克隆内**打 2 行 POSIX 兼容补丁（与原仓脏树中用户自己的未提交修复逐字同构；补丁前后 sha256 见 `baseline_step2b_patch_provenance.log`）后重跑：

```
[k-1-5] total=164 failed=0      （step2b_exit=0）
```

**→ 164 道宪法门禁在 main 锚点 + macOS 上语义全绿。**

### 0.3 门禁基准数完整性（R6：门禁数≠预期 的变体）

164 manifest 条目 ↔ 164 个 `tests/constitution_*.rs` 文件，双向 set-diff 为空（`gate_census.md`）。但逐文件扫描 `#[test]` 计数发现 **1 个零测试占位门**：`tests/constitution_real17_evaluator_prompt_provenance_wire.rs`（9 行，0 个 `#[test]`，文件头自述"source-grep tests removed… retained as a placeholder"，evaluator 已在 309e026a 删除）。其余 163 个均含 ≥1 个真实测试。
**移植验收基准数应记：164 个门禁文件被发现并跑、其中 163 个可失败、1 个已知占位**（"不会失败的门禁是文档不是门禁"——AGENTS.md §7 原文）。

### 0.4 关于前置 #2 终止条款的处理（透明声明）

执行信封前置 #2 规定"跑不绿→立刻终止审计"。执行裁量：4 个红已全部归因（1 簇语义红×CI 盲区、1 flake、1 平台红），且 164 门禁——本仓宪法正确性的主判据——语义全绿，故按 R6（"测试红→升级为 memo 显著章节"）继续完成 §2-§6，全部后续结论均以"红基线已编目"为前提条件。若架构师不认可此裁量，§③-§⑤ 可整体作废，§0-§② 证据独立成立。

---

## ① 库存与门禁基准数

| 项 | 值 | 证据 |
|---|---|---|
| 实现 LOC（src，wc -l 法） | 124,541 | `inventory.md` / `test_structure.md` |
| 测试 LOC（tests/） | 93,507（比值 0.75）；374 个集成测试文件 | 同上 |
| 宪法门禁 | **164**（163 可失败 + 1 占位，见 §0.3）；跑器 fail-closed 双向核对 | `gate_census.md` |
| workspace 结构 | 2 members：root `turingosv4` + `spike/gix_capability`；**exclude `experiments/minif2f_v4`** | `inventory.md` |
| bin 目标 | **37**（1 显式 `turingos_web` + `src/main.rs` 隐式 + 35 个 src/bin 自动发现）；`turingos` CLI 为无 clap 手写 dispatcher，29 个子命令 | `module_map.md` |
| 最大文件 | sequencer.rs 9,592 / typed_tx.rs 5,633 / audit_assertions.rs 4,066 / audit_dashboard.rs 3,598 / cmd_generate.rs 3,099（前两者 = AGENTS.md §6 受限 Class-4 候选面） | `inventory.md` |
| 依赖健康 | 22 个顶层依赖；`cargo audit`：0 漏洞、1 条 unmaintained 警告（bincode RUSTSEC-2025-0141，仓内已 allow）；重复依赖仅 getrandom v0.2.17/v0.4.2 | `cargo_audit.txt` / `cargo_tree_duplicates.txt` |
| 非 Rust 移植面 | frontend/ 41 文件（TS 工具链独立）；scripts/ 74 个（含门禁跑器+git hooks）；trust-root pins 在 `genesis_payload.toml` | `inventory.md` |
| 移植锚点 | `1f00012d`（main）；备注：`claude/p1-realvalue` 领先 143 commits 未入审 | §0.1 |

六个审计模块组定位（cargo-mutants 路径级，定位证据全文在 `module_map.md`）：replay 验证=`src/runtime/verify.rs`（fn verify_chaintape :230，7 指标 ReplayReport）；签名验证=`src/bottom_white/ledger/system_keypair.rs`（verify_system_signature :586）；tape/CAS 追加=`src/bottom_white/ledger/transition_ledger.rs`（fn append :299 root-fold）+ `src/bottom_white/cas/store.rs`；sequencer=`src/state/sequencer.rs`（struct :4981）；market=`src/economy/*` + sequencer 准入 fns；CLI=`src/bin/turingos.rs` + `src/bin/turingos/`。

---

## ② 机器扫描数据表（原始输出全部在 `audit_data/machine_sweep/`）

**clippy 全量**（`cargo clippy --all-targets`，不 -D）：**227 条 warning + 3 条 error 级**，exit 101。
- 分布 top：doc 缩进 43 / 可派生 impl 30 / map 值迭代 16 / or_default 类 15 / Cid Copy 仍 clone 7 / 参数过多 7（`clippy_distribution.txt` 全表）。风格债为主，无 correctness lint 群。
- 3 条 error 级（deny-by-default lint，rustc 编译可过）：`sequencer.rs:1287/:1297` `never_loop`（实读：`for pid in expected.difference(&got) { return Err(…) }` 故意"首失配即返回"模式，语义正确、风格异常，落在 Class-4 面上）；`autopsy_capsule.rs:396` `absurd_extreme_comparisons`（`timestamp_logical >= TB15_…_T` 而常量=0，配置性恒真激活门）。
- **推论：AGENTS.md §7 自检命令 `cargo clippy` 在 main 上非绿**（与 workspace 测试一样不在 CI）。

**unsafe 普查**：实 unsafe 块 **4**，全部单表达式 libc FFI（chain_tape_lease.rs:295/:312/:318 进程活性+errno，**无 SAFETY 注释**；system_keypair.rs:1118 mlock，有 SAFETY）。无 unsafe fn/impl/trait；**无 `forbid(unsafe_code)` lint 配置**。判定：**无 unsafe 聚集**（S3 第三腿对全模块为否）。

**panic 面**（实现代码，排除 tests/ 与 #[cfg(test)]）：impl 总计 **112** vs 测试代码 5,924。top 文件几乎全为文件内测试模块；真实热路径集中在：`sequencer.rs` impl 38 处（26 个 canonical_encode expect + **1 个无条件 `panic!` :874**（pub 包装器，生产路径用 _checked 变体）+ 9 个不变量背书 expect）；`boot.rs` 启动路径 7 处（启动即死型，可接受）；`typed_tx.rs`/`agent_keypairs.rs` 各 1 处热路径 expect。全表+逐文件分类在 `panic_census.md`。

**测试结构**：property testing（proptest/quickcheck）使用点：**0**。164 门禁 + 374 集成文件全部是 example-based。

**热点图**（修改频次×体积 top10，全表 `hotspot_map.md`）：

| 文件 | touch | LOC | 类别 |
|---|---|---|---|
| src/runtime/mod.rs | 65 | 1,186 | other（trust-root 钉死面） |
| src/state/sequencer.rs | 60 | 9,592 | **sequencer** |
| src/state/typed_tx.rs | 42 | 5,633 | other（canonical signing payload） |
| src/bottom_white/ledger/transition_ledger.rs | 34 | 2,474 | **tape** |
| src/runtime/audit_assertions.rs | 31 | 4,066 | other |
| src/bin/turingos/cmd_generate.rs | 29 | 3,099 | **cli** |
| src/bin/audit_dashboard.rs | 28 | 3,598 | other |
| src/economy/monetary_invariant.rs | 24 | 1,055 | other |
| src/sdk/prompt.rs | 19 | 663 | other |
| src/state/q_state.rs | 18 | 1,138 | other |

（signer=system_keypair.rs touch 15 列第 18、cas/store.rs touch 15 列第 19。）

**CI 覆盖空缺汇总**（移植后必须补的验收面）：CI 仅跑门禁套件+closure_3+matrix 文档存在性；无 workspace 测试、无 clippy、无 fmt、无 macOS lane。

---

## ③ 变异抽样（测试深度硬证据）

**测量口径（先于结果声明）**：`cargo mutants -f <模块> --timeout 60 --no-shuffle -j 2`（runbook §3 原方）。每 mutant 的测试命令 = **lib 单元测试 + 关键词匹配的宪法门禁**（`--lib --test constitution_*<相关>*`），并 `--skip verify_trust_root --skip sandbox_cwd_is_explicit_workspace` 排除 §0.2 已编目红使 baseline 可过。全套 374 个集成二进制每 mutant 重链接在 4h CPU 预算内不可行（R4），故本测量是**真实测试网的下界采样**：missed 比率为真实生存率的**上界**（被排除的集成测试可能接住部分 missed mutant）。结论按此口径解读，不冒充全网深度。

| 模块 | 文件 | mutants | caught | missed | unviable / timeout | **生存率** | 判读（>25% 浅 / <15% 可托底） |
|---|---|---|---|---|---|---|---|
| replay 验证 | src/runtime/verify.rs | 38（**完成**，57min，baseline 81s build+22s test） | 12 | 3 | 3 / 20 | 结论集上界 **3/15 = 20.0%**（timeout 计为行为偏差检出则 3/35 = 8.6%） | **15-25% 边界带**——不达"<15% 可托底"、未越">25% 浅"，标'边界'按保守侧解读 |
| 签名验证 | src/bottom_white/ledger/system_keypair.rs | 123 中测得 99（**预算中止**，baseline 45s build+19s test） | 9 | 7 | 20 / 63 | **无法机械判定**：结论集 7/16 = 43.8%（>25%）vs timeout 计检出 7/79 = 8.9%（<15%）——两种合法解读横跨全部阈值 | **降级声明**：60s 上限下 timeout 主导（63/99；baseline test 仅 19s，timeout 多为 mutant 致挂死=行为偏差，但口径无法机械裁决）。**missed 清单本身是硬证据**（见下） |
| tape 追加 | src/bottom_white/ledger/transition_ledger.rs | **未运行**（R4 预算硬顶） | — | — | — | **证据缺失（降级）** | §⑤ 对 tape 的 S2 裁决经由边界规则触发，不依赖生存率 |

**signer 的 7 条 missed（全文 `signer_keypair_missed.txt`）含两条签名面要害**：`system_keypair.rs:81` `fingerprint_sha256 → [0;32]/[1;32]` 未被接住（公钥指纹无金标断言）；`:362` `from_plaintext` 中 `!=` → `==` 比较子翻转未被接住（密钥材料长度/格式校验比较子无对抗测试）。这两条与 §④ 双 critic 独立收敛的"金标向量缺失"发现**三方互证**：签名模块测试网在细节断言上确有可证伪的洞——这是"补测试先行于任何整修"的机械依据。replay 的 3 条 missed 中 `verify.rs:410` 删除 `TypedTx::Work` match 臂未被接住（工件验证臂保护薄弱），同性质留痕。

**预算执行记录（R4）**：变异 01:40 起跑；执行层于 07:03 进度检查时发现已超 4h 硬顶（应停于 ~05:40），随即 kill（07:05）。超额时段产出 = signer 部分样本（保留为证据，不再续跑）；tape 未启动。原始清单：`audit_data/mutants/<module>_{caught,missed,timeout,unviable}.txt` + `outcomes.json` + `*_run.log`。

---

## ④ 双 critic 结构化评审（§4）

**独立性证明**：两个 clean-context 子代理，prompt 逐字相同（唯一差异 = critic 代号 A/B 一个 token），并行执行互不可见，禁读对方输出目录。10 份原始评审件：`audit_data/critics/critic_{A,B}_{sequencer,tape_append,signer,cli_generate,control_sdk_prompt}.md`。
**选样规则执行**：热点 top ∩ {replay, tape/CAS, sequencer, signer, CLI} —— replay 未进热点 top30，交集 = {sequencer.rs(60), transition_ledger.rs(34), cmd_generate.rs(29), system_keypair.rs(15), cas/store.rs(15)}，按"每类别取触碰最高者"取 4（tape 类内 transition_ledger 34 > store 15）；随机对照 = 锚点十六进制 `0x1f00012d mod 11`（候选池=热点 top20 中非五类别 src 实现文件，按热点序）→ 第 5 项 = **src/sdk/prompt.rs**。

**评分矩阵**（1-5，5 优；六维 = 边界可包裹/错误纪律/不变量文档化/测试深度/并发安全/死代码密度）：

| 模块 | critic | 边界 | 错误 | 不变量 | 测试 | 并发 | 死代码 | 均分 |
|---|---|---|---|---|---|---|---|---|
| sequencer.rs | A | 3 | 4 | 5 | 5 | 4 | 5 | 4.33 |
| sequencer.rs | B | 3 | 4 | 4 | 4 | 4 | 5 | 4.00 |
| transition_ledger.rs | A | 3 | 4 | 5 | 4 | 3 | 5 | 4.00 |
| transition_ledger.rs | B | 4 | 4 | 5 | 4 | 4 | 5 | 4.33 |
| system_keypair.rs | A | 4 | 5 | 4 | 4 | 4 | 4 | 4.17 |
| system_keypair.rs | B | 4 | 4 | 4 | 3 | 4 | 4 | 3.83 |
| cmd_generate.rs | A | 3 | 4 | 4 | 4 | 4 | 4 | 3.83 |
| cmd_generate.rs | B | 3 | 4 | 4 | 4 | 4 | 5 | 4.00 |
| sdk/prompt.rs（对照） | A | 3 | 4 | 3 | 4 | 3 | 3 | 3.33 |
| sdk/prompt.rs（对照） | B | 3 | 3 | 3 | 3 | 3 | 3 | 3.00 |

**分歧（原样列出，不调和）**：每维 A/B 分歧全部 ≤1。具体分歧点：sequencer 不变量(5/4)与测试(5/4)；tape 边界(3/4)与并发(3/4)；signer 错误(5/4)与测试(4/3)；cli 死代码(4/5)；prompt 错误(4/3)与测试(4/3)。无任何 ≥2 分对立。对照组 prompt.rs 双双垫底（3.00-3.33）——对照有判别力，rubric 非恒高。

**RiskFinding 总量 68**（A 35 / B 33；JSONL 原文嵌于各评审件）。**risk 级 3 条，已由审计官亲核证据**：

1. `rsk_verify_system_pubkeys_fail_open_stub`（B risk / A attention，**双方独立命中**）：`system_keypair.rs:617-624` `verify_system_pubkeys` 对存在 `[system_pubkeys]` 的 genesis **无条件 `Ok(())`**（TODO(CO1.7) 注释），签名面上的 fail-open 占位。亲核：原文照录属实。
2. `rsk_prompt_builder_orphaned_no_production_caller`（B risk / A attention，双方独立命中）：`src/sdk/prompt.rs:48` `build_agent_prompt` ——被多个宪法门禁守护的"canonical prompt 面"——在锚点上**无任何非测试调用方**（生产消费者 evaluator 已随 309e026a 删除）；而真实发往 LLM 的 prompt 在 `src/bin/market_external_agent_current_kernel.rs:423` 的**同名私有函数**里构建，处于所有 shielding 门禁扫描范围之外。
3. `rsk_dual_fence_parser_drift`（A risk）：cmd_generate 的双 fence 解析器漂移风险（详见 critic_A_cli_generate.md，引用齐全）。

**跨 critic 独立收敛的 attention 级发现**（独立命中 = 强信号，全部留痕原文于评审件）：sequencer pub panic 包装器(:867-876)与生产 _checked 变体并存、CAS 锁中毒静默降级(:919-921，与自身 doc 矛盾)、`replace_q_for_test` 以 `#[cfg(debug_assertions)]`（非 cfg(test)）暴露**整个 QState 注入孔**(:7101，debug profile 嵌入即携带)、过期"Stub state"模块头(:11-15)；tape 金标 wire 向量缺失(双方)、git2 多实例 ref race/单写者假设无锁背书(双方)、legacy decode 默认 outcome=yes 注入(双方)；signer KDF header 未认证(双方)、mlock 失败静默+无 munlock(双方)；prompt `TURING_STEP_ONLY` 旗舰分支零测试(双方)、env 变体静默回落 v0(双方)、**占位门禁 real17 注册于 manifest**（B，已并入 §0.3 亲核）。

---

## ⑤ 模块决策表（§5 预注册规则机械映射）

**聚合约定（执行层声明）**：双 critic 分数取**较低者**（保守侧，与"边界案例按保守侧"同精神）；"宪法关键"= runbook §1 列举的 replay/门禁/验签/append-only 纪律所在模块；"unsafe 聚集"按 §② = 全员否；触碰权重 = runbook §1（P2 碰 signer/verifier、P3 碰 ratify/tag、P6 碰 replay/sequencer）。

| 模块 | 宪法关键 | 边界(min) | 均分(min) | 非测试 panic 面 | 并发(min) | 变异生存率(上界) | **裁决** | 规则依据 | 触发 Atom |
|---|---|---|---|---|---|---|---|---|---|
| sequencer.rs | 是 | 3 | 4.00 | 38（热路径） | 4 | （未抽样；典型 9.6k 行巨石） | **S1（默认）+ 留痕** | S1 严格条件败于边界 3<4 与 panic 面≠0；S2 败于均分 4.00≥3；S3 败于并发 4>2 ∧ 无 unsafe → 落入"其余→默认 S1+RiskFinding 留痕" | 首个 P6 触碰 Atom 顺手：panic 包装器去 pub 化、锁中毒降级改 fail-closed、过期模块头（全部已留痕 §④） |
| transition_ledger.rs | 是 | 3（A3/B4 跨阈值） | 4.00 | 0 | 3 | 未测（预算硬顶，降级） | **S2（边界→保守侧）** | 边界维恰好骑在 S1 决定性阈值上（A=3/B=4）→ 按"边界案例标注'边界'并按保守侧（S2）归类"；在 P6 触碰路径上 | 首个 P6 触碰 Atom：先补金标 wire 向量+git2 单写者锁背书（双 critic 收敛缺口），再动实现 |
| system_keypair.rs | 是 | 4 | 3.83 | ~0（census top10 之外；UNVERIFIED 精确值） | 4 | 无法机械判定（§③：43.8% vs 8.9% 横跨阈值） | **S1（默认）+ 留痕（标'边界'）** | S1 严格条件（宪法关键→生存率<15%）证据不具判别力→无法认证严格 S1；S2 败于均分 3.83≥3；S3 败于并发 4>2 ∧ 无 unsafe 聚集 → 默认 S1 + 留痕 | P2 触碰 signer 时顺手，且**补测试先行**：指纹/from_plaintext 金标向量（§③ missed 实证）+ fail-open stub（CO1.7 TODO）是**第一颗 P2 Atom 必须直面的留痕项** |
| runtime/verify.rs（replay） | 是 | （未评审：未进热点 top∩） | — | 低（census top10 之外） | — | 20.0%（结论集上界；timeout 计检出则 8.6%） | **S1（默认）+ 留痕（标'边界'）** | S1 严格条件（宪法关键→生存率<15%）：保守解读 20% 不满足；S2 缺评审分数且需均分<3 → 不可用；S3 败于无 unsafe 聚集 → 默认 S1 + 留痕，生存率落 15-25% 带标'边界' | P6 replay 触碰 Atom 顺手：补 `TypedTx::Work` 验证臂断言（§③ missed 实证） |
| cmd_generate.rs | 否（CLI 层） | 3 | 3.83 | 中（CLI 启动路径为主） | 4 | （未抽样） | **S1（默认）+ 留痕** | 非宪法关键；S2 败于均分≥3 → 默认 S1 | CLI_ABI 适配是独立车道（§⑥），非绞杀整修 |
| sdk/prompt.rs（对照） | 否 | 3 | 3.00 | 低 | 3 | （未抽样） | **S1（默认）+ 留痕（显著）** | B 均分恰=3.00 不满足"<3"；不在 P2-P8 触碰表 → S2 不可用 → 默认 S1。**但 orphaned-builder 事实（§④ risk#2）必须在移植清单中显著标注：照搬此模块 = 移植一个无生产调用方的被门禁守护面** | 移植时连同 shadow builder 一并裁决（保留/废弃属用户域） |

**S3 判定：0 个模块。** 三条件（宪法关键 × 生存率>25% × unsafe 聚集/并发≤2）在本仓无交集——unsafe 全员无聚集、并发最低分 3。runbook"预期极少"成立到极致：**没有任何模块需要移植即重写**。

**Goodhart 声明**：以上阈值未在看到数据后调整；两处执行层裁量（min 聚合、边界跨阈值→S2）均在数据落盘前的约定内并已原文披露。

---

## ⑥ CLI_ABI 七律差距清单（对照 docs/CLI_ABI.md；活体探针 + 代码证据，全文 `cli_abi_precheck.md`）

| 律 | 裁决 | 证据要点 |
|---|---|---|
| 1. 命令必须支持 --json | **FAIL** | 29 个子命令中**完全支持 1 个**（audit dashboard），部分/默认 JSON 8 个，**无 JSON 路径 20 个**；`report run --json` / `verify chaintape --json` 被硬拒 exit 2（"unknown arg: --json"） |
| 2. stdout 纯 machine JSON | **FAIL** | JSON 纯净岛存在（verify chaintape happy path 实测 stdout 纯 JSON、stderr 空）；但 init/config/agent/batch/welcome/export/spec 输出散文，replay --offline 混排 prose+JSON |
| 3. stderr 仅诊断 | PARTIAL | 全部实测错误正确落 stderr 且 stdout 干净；但 spec audit 把业务裁决放 stderr，audit tape/tamper 把人读裁决放 stdout |
| 4. schema_version 必带 | **FAIL** | 全仓 0 处 CLI 响应带 schema_version（rg "tos.app.cli" exit 1）；按律 4 fail-closed 语义，**今天每一条响应都解析失败** |
| 5. typed error code | **FAIL** | 所有错误 = 散文 stderr + 粗 exit 2；最接近的是 llm complete 的 `{"ok":false,"error":{"kind",…}}` 单点 |
| 6. 长任务事件流/回执轮询 | PARTIAL | 无 --follow/--events/--stream；generate 写侧信道 `generate_progress.jsonl`（可作回执基底但非 CLI 契约）；generate/llm/tdma 阻塞 stdout 至完成 |
| 7. fixture transcript | **FAIL** | 仓内无 fixtures/cli_transcripts；本次探针 transcript 可作种子 |

**额外库存事实**：6 个子命令（task open/view/tick、report wallet/positions/bankruptcy）依赖被 workspace **exclude** 的 `experiments/minif2f_v4` 的 lean_market 后端——**在 main 锚点构建下运行即败**，且内置补救提示已过期。
**正面证据**：dispatcher 无一探针 panic、stdout/stderr 分离纪律一致、exit 0/1/2 分类税制稳定（cmd_verify_chaintape.rs:31-35）——适配层有可靠地基。
**移植含义**：CLI_ABI 合规是**七律全面改造**而非逐命令小修；adapter 车道 A 初期只能白名单 verify chaintape 等 JSON 岛，其余按"non-conformant 隔离适配 + 车道 B 推上游"处理（CLI_ABI.md 第 3 段既定策略的真实工作量现已量化）。

---

## ⑦ 移植三参数建议（供用户停机点终裁；每项附理由与备选）

**参数 1 — subtree 历史保留方式：建议 squash 单 commit 导入锚点，commit message 记录 `turingosv4@1f00012d`；v4 原仓保留为历史参照。**
理由：(a) "完整历史"此刻是伪命题——143 个未合并 commits + 6.5k 行未提交工作意味着 v4 的真实历史仍在 v4 仓续写，subtree 全历史合并只会带进一半故事；(b) turingos.app 的 repo 法律（shipgate/hooks/contracts）对入仓代码生效，5k+ 历史 commits 不经这些门禁，污染 blame 与 CI 语义；(c) 可溯性由"锚点 rev 写入导入 commit + v4 仓存档"保障，本审计的热点图（依赖完整 git log）证明历史的诊断价值在**原仓**消费即可。备选：`git subtree add --squash`（等价）或全历史 subtree merge（保 git log 连续性，代价如上）。

**参数 2 — v4 原仓命运：建议移植基线绿后冻结新功能（archive 或 main 分支保护+只收 PR-split 整理），唯一真相迁至 turingos.app。**
理由：(a) 双活内核必然漂移，违背"App is the sovereign projection over truth"的单一真相原则；(b) `claude/p1-realvalue` 的 143 commits 已有用户自写的 PR-split 计划（`handover/reports/UNMERGED_WORK_DEEP_SUMMARY_2026-06-02.md`，HEAD commit 即为 auditor/architect 准备）——该计划应改道为**新仓 atoms**逐颗落地或显式废弃，而非继续在 v4 仓积累；(c) 6.5k 行未提交工作含门禁跑器 macOS 修复等真价值，冻结前需按该文档完成一次性 triage（用户域裁决）。

**参数 3 — 执行路径：建议 S1-first 四步，且基线定义需用户批准一处修订。**
1. `A1_9_1` squash 导入 `turingosv4@1f00012d` 为 workspace 成员（零改动原样导入）。
2. `A1_9_2` 门禁跑器可移植性修复（本审计已验证的 2 行 POSIX 补丁，`baseline_step2b_patch_provenance.log` 有前后哈希与绿证）+ macOS 本地/CI lane + 把 `cargo test --workspace` 纳入 CI（堵 §0.2 的盲区）。
3. **基线判定 = 164 门禁绿 + matrix drift 绿 + workspace 绿（除 4 个已编目红 R-1a/b/c、R-2、R-3）**。⚠️ 基线保护原则原文要求"全部测试跑绿"，但 main 锚点自身红（§0.2），该原文在锚点上**不可达**——此修订是本 memo 请求用户显式批准的唯一原则级偏离。
4. 基线绿后小 PR 序列：`A1_9_3` trust-root pin 重哈希修复（judge.sh；**Class-4 trust-root 面，须 per-atom §8 ratification**，且依"never fix a trust-root hash mismatch"纪律此修复只能由用户授权发起）→ `A1_9_4` sandbox_cwd macOS canonicalize → `A1_9_5` tempdir flake 隔离 → 绞杀清单（§⑤ transition_ledger S2 项 + 各留痕项按触碰顺序）。
随后解锁 P2+（CLI_ABI 七律改造按 §⑥ 车道策略独立排程）。

---

## 完工自检（逐项核对）

- [x] v4 基线测试+门禁结果如实记录：基线红已作头条（§0.2 失败原文逐字；164 门禁补丁后语义绿 `[k-1-5] total=164 failed=0`）
- [x] 门禁基准数已盘点并核点：164 manifest=164 文件、双向 diff 空、1 个零测试占位门已揭示（§0.3）
- [x] 三模块变异生存率：replay 完成（38/38，结论集 20.0%）；signer 部分（99/123，两种解读横跨阈值→无判别力声明）；tape **未跑**——R4 预算硬顶执行（实际停于 07:05，超顶时段披露于 §③），**变异证据降级已显式标注**
- [x] 双 critic 独立性可证明：10 份原始件在 `audit_data/critics/`，prompt 逐字相同唯 critic 代号异，并行互不可见
- [x] 每个模块的 S1/S2/S3 决策引用 §5 具体规则条目（§⑤ 表"规则依据"列）
- [x] CLI_ABI 七律差距表完成（§⑥，活体探针 + 代码证据）
- [x] 三参数建议各附理由与备选（§⑦）
- [x] memo 已 pull-first 提交推送：`4549438`（"R1.9: port-readiness audit memo (evidence-driven S1/S2/S3 map)"，main `d019d0a..4549438`）

**审计纪律自证**：turingosv4 原仓零写入（审计全程在 `--no-hardlinks` 克隆内；唯一执行态修改 = 克隆内门禁跑器 2 行补丁，带前后哈希取证）；对 turingos.app 仓的写入仅 `research/R1.9_memo.md` + `audit_data/**`；无补丁、无建议代码产出；所有数字可溯源至命令+输出文件。
