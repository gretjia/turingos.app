---
atom: A1_63b_railroad_layout
timestamp_utc: 2026-06-16T00:30:00Z
git_head_base: c9ad6a4c (origin/main after A1_71 #54)
implementer: orchestrated — Opus(orchestrator/auditor) locks design; Sonnet implements; Opus+Haiku adversarial review; Opus runs gate+ships
---

# A1_63b — Railroad Swimlane 布局(扇形→树)

## 交付
用户 #1 诉求:galaxy 的扇形(branch 极坐标轨道发散)换成 **Railroad Swimlane 树**——主线竖直脊柱、分支平行 lane、commit 拓扑序、细线 rail。

**编排(用户授权:workflow + Sonnet/Haiku 重活 + 我 orchestrator/auditor)**:
- **map+design workflow**(wf_516d0c7b):4 Sonnet reader map 现状(scene/layout/render/golden)→ Opus 设计railroad 算法+接口契约。
- 我(orchestrator)审定锁定 5 个开放问题:① 跨 lane 边=直对角线(rail 弯路推迟 A1_63c);② **parent 边 alpha 0.08→0.30**(脊柱/rail 可见=用户核心诉求);③ 全 dot 不 fold;④ 单 trunk MVP,森林降级不崩;⑤ 复用 60/80/200。
- **implement+review workflow**(wf_a6c26e31):Sonnet 实现(迭代至 swift build+test 绿+golden 重生)→ Opus 正确性/诚实律 + Haiku 机械审计(并行)。

**实现**(RadarLayout.positions step1 galaxyCenters + step2 worktree 原样;替换 step3 极坐标扇 + step4 per-branch ts swimlane):
- 纯函数 `RadarLayout.railroadLanes(branches:commits:) -> (rowOf,laneOf,branchLane)`(单项目):**Kahn 拓扑排序** over parentShas(in-scene 父计 in-degree;ts+sha tie-break)→ 全局 rowOf;trunk(isDefault=A1_63a 真 main)first-parent 链 → lane 0 脊柱;非 trunk pvigier nil-slot 复用(lane≥1,**lane 0 保留**:复用池 `$0>=1` + 父延续 `pl>=1` 守卫,非 trunk 永不上脊柱);branch=lane 头。
- positions:x=center.x+lane*60;y=center.y+200+row*80。常量 LANE_WIDTH/ROW_SPACING/RAIL_TOP_OFFSET;退役仅扇用的 star* 常量。
- GalaxyRenderer:.parent 边 alpha 0.08→0.30(GPU 值不入 canonicalDump)。RadarScene.derive 不改。
- 森林降级:0 isDefault→无脊柱各 lane≥1 互异;>1→各占一 lane(co-trunk 全 chrome 推迟)。

## 验证
- **shipgate p1 16/16 PASS**(a163b_shipgate.log;gate 12 golden 重生已提交并 match;gate 16 app lane 绿+dist 重建带 railroad)。app 32 RadarModelTests 全绿。
- **纯 railroadLanes 机械测 6 例全绿**:trunk 直脊柱(a0/a1/a2 全 lane0 row 递增)/分支 lane≥1≠trunk/**拓扑序非 ts**(子 ts 早于父但 row 仍在父之下=ADR-017 诚实律)/tie-break commitSha 升序+canonicalDump 确定性/偏 DAG 有限坐标不崩/森林 0&>1 isDefault 不崩不塌 lane。
- **golden 诚实重生(审过非盲接)**:positions 入 canonicalDump → p1_galaxy_scene.golden 变。Opus **手算 4 坐标逐字节核对**:main lane0 x=-1990.9 / feat/wip lane1 x=-1930.9 / a0 lane0 row0 y=2023.8 / a1 lane1 row1 y=2103.8 —— 全 match=真 railroad 坐标非掩盖 bug。**p1_radar_scene + a1_09_mixed golden 逐字节不变**(worktree-only,证 step2 未扰)。galaxyCenters/MIN_GALAXY_GAP 不变。
- **对抗双审 SHIP×2**:Opus 正确性/诚实律 SHIP(确认 topo-not-ts、lane0 保留无 fake-merge、first-parent 脊柱、偏 DAG、森林降级、golden 诚实、worktree golden 不变、build 净);Haiku 机械审计 SHIP(allowlist 合规、worktree golden 0 diff、alpha=0.30、railroadLanes 签名、6 测在名、32/32 绿、常量、CURRENT)。

## 诚实交代(报忧 / 残留,非 blocking)
- **真机像素截屏未由我拿到**:app 窗口 Space 漂移 + SCContentFilter 环境墙([[reference_locked_mac_live_verify]]),非代码。代偿:① railroadLanes 6 机械测钉死脊柱/lane/topo 几何;② golden 4 坐标手算 byte-match=railroad 坐标实证;③ 渲染路径(Metal dot+A1_71 线)与用户先前已目视的同;④ 双审 SHIP + gate 16/16。dist 已重建,**用户 zoom in turingos.app 即见竖直脊柱+平行 lane**。
- **Opus nit(非 blocking)**:trunk first-parent 链中段有 off-scene gap 的情形未单测(行为诚实=断链不成连续脊柱,深祖先落非 trunk lane);可后续补测。
- **DEFER(已锁,非本 MVP)**:跨 lane 边 L/S-curve rail 路由(A1_63c)、commit fold、co-trunk 全 chrome(A1_63a2)、drawProjectLane 旧横轨替换、常量 retune(真机后)。

## shipgate p1 输出
[PASS] 1-16 全绿(见 a163b_shipgate.log);SHIPGATE p1: PASS。golden(p1_galaxy_scene)诚实重生已提交;worktree golden 不变。
