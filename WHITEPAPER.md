# Turing Agentic OS 白皮书

**给你的 AI agent 装上同意、可见性与回执 —— 一个保护用户的 macOS 治理层**

版本 v0.1 · 2026-06-11 · 状态：双线调研已收束（Apple 平台 75 条论断 / 生态 84 条论断，对抗复核后入稿）

> 配套文档：[FEASIBILITY.md](FEASIBILITY.md)（每条外部论断的 verified/unverified 标注与来源）。

---

## 0. 一句话

**Turing Agentic OS 是 macOS 上的个人 agent 治理层：你的 AI agent 自愿经由它行动，你因此获得可见性、逐项批准、可回放的历史，以及——凡可撤销处——一键还原。**

它不生产智能。它生产的是**你敢把事情交给智能的那份放心**。

## 1. 我们要解决的问题

2025–2026 年，个人 AI agent 从"会聊天"进入"会动手"：读写文件、收发消息、操作浏览器、执行命令、安排日程。OpenClaw、Hermes、Claude Code、Codex 这类工具让一个人晚上派活、早上验收成为现实。

但"会动手"带来一个朴素的问题，每个用户迟早会问：

> 我敢不敢让它碰我的邮箱、文件、账户？它昨晚到底做了什么？做错了能不能撤回？

今天的诚实答案是：**你只能选择信或不信。** agent 的动作没有统一的可见面，没有"做之前先问我"的仪式，没有用户自己拥有的操作历史，更没有还原键。各家 agent 内部各有安全机制（这很好，我们会如实介绍），但它们彼此割裂、形态不一，且都由 agent 自己记账。

Turing 的回答：在 agent 与你的真实世界之间，放一个**你拥有的、中立的、本地的**治理层。

## 2. 设计操守（产品的不可谈判项）

这一节先于一切功能存在。违反任何一条的功能，不做。

1. **只治理自愿接入的 agent。** Turing 只看得见、管得住那些被用户配置为经由 Turing 网关行动的 agent（通过 MCP——Model Context Protocol，agent 连接工具的开放标准——或 CLI 包装配置接入）。我们不会、也无法静默监管你电脑上的一切——**我们绝不作此声称**。覆盖边界（什么在网关之外）在本文 §7 与每个集成的文档中如实披露。
2. **不可逆动作默认是草稿。** 文件编辑这类可撤销的本地动作，可以在隔离工作区暂存、批准后应用。但发邮件、付款、发帖这类对外动作**无法真正撤回**——所以唯一诚实的设计是 draft-by-default：先存为草稿，由你用 Touch ID 确认后才执行。我们不向你承诺"撤回一封已发出的邮件"这种工程谎言。
3. **批准记录的是"你当时看到了什么"。** 每一次批准，连同批准界面所呈现内容的摘要与哈希一起记录。事后回看时，你看到的不只是"批准了"，而是"基于什么批准的"。
4. **对 agent 诚实。** 当一个动作被暂存而非执行，网关返回给 agent 的结果必须如实说明（staged / 待批准），不让 agent 误以为动作已完成——否则它后续的计划会建立在假事实上。
5. **本地优先。** 历史记录（黑匣子）存在你的电脑上，归你所有，可导出、可回放。Turing 不是一个把你的操作上传到别处的服务。

## 3. 四个支柱：同意 · 可见性 · 审计 · 可撤销

### 3.1 同意（Consent）

接入是一个明确的、用户发起的动作：你把自己的 agent 配置为使用 Turing 提供的工具。没有暗中代理，没有"装上就接管"。每个 agent、每类动作的授权策略由你设定：

- **自动允许**：低风险只读动作（如读取你指定目录的文件）。
- **自动暂存**：可生成草稿的动作（写邮件草稿、拟日程）。
- **需要批准**：有后果或难以撤回的动作——弹出批准卡，Touch ID 确认。
- **直接拒绝**：你划出的禁区（如触碰特定目录、特定收件人）。

### 3.2 可见性（Visibility)

一个安静的菜单栏入口回答一个问题："**现在需要我吗？**"——需要你裁决的事置顶，进行中的事呼吸，一切安静时界面近乎空。agent 经由网关的每个动作都有一行人话结论，可下钻到原始证据。

### 3.3 审计（The Black Box）

每个经由网关的动作——包括被拒绝和失败的——都落入一份**你拥有的**、追加式的本地历史：谁（哪个 agent）、想做什么、基于什么、结果如何、你是否批准、批准时看到了什么。历史可回放：你可以像翻黑匣子一样重看任何一晚发生的事。

### 3.4 可撤销（Undo where honest）

- 文件类动作在隔离工作区先行发生，批准后才落到真实目录；落地后保留还原点，一键还原。
- 还原点由 Turing 暂存工作区自身的版本化提供；系统原生版本机制（如文稿版本）凡确实可用处叠加利用——我们不依赖、也不承诺系统级快照还原（原因见 §6.2）。
- 凡是无法撤销的（已发出的邮件、已提交的付款），Turing 从一开始就不让它"先发生再说"——见 §4 第三类动作。

## 4. 三类动作：一张诚实的风险地图

所有经由网关、**会产生副作用**的动作被分为三类（只读动作是零类：默认自动允许，但同样全部进黑匣子）。**分类即策略**——类别决定了它默认怎么被对待。

| 类别 | 例子 | 默认处置 | 撤销真相 |
|---|---|---|---|
| **零类：只读动作** | 读取指定目录文件、查日程 | 按策略自动允许；全部入黑匣子 | 无副作用 |
| **一类：可逆本地动作** | 移动/编辑文件、整理目录、改代码 | 在隔离工作区先行执行；批准后应用到真实位置；保留还原点 | 真撤销（应用前丢弃 / 应用后还原） |
| **二类：可暂存的远端草稿** | 邮件草稿、暂定日程、文档草稿 | 直接以"草稿"形态创建在目标系统；最终发送/确认仍属三类 | 草稿可删；不假装"发送可撤销" |
| **三类：不可逆外部动作** | 发送邮件、付款、发帖、提交表单、删除远端数据 | **永远不自动执行。** 生成批准卡，等待 Touch ID；批准内容（收件人/金额/正文摘要）进入签名记录 | **无法撤销，所以前置同意是唯一保护** |

批准卡上永远写明动作属于哪一类、能否撤销。我们不用模糊的"安全/不安全"，而用"可逆/不可逆"这种可被检验的语言。

## 5. 协作式网关架构

```
你的 agents（OpenClaw / Hermes / Claude Code / Codex / …）
        │  自愿接入：MCP 配置 / CLI 包装 / hooks
        ▼
┌─ Turing 网关（本地进程，无云依赖）─────────────┐
│  策略引擎：四档处置（允许/暂存/批准/拒绝）        │
│  三类动作分类器（声明式，工具签名即类别）          │
│  隔离工作区（一类动作的先行区）                  │
│  批准仪式（Touch ID + 所见内容入记录）            │
│  黑匣子（追加式本地历史，可回放可导出）            │
└──────────────┬───────────────────────┘
               ▼
        你的真实世界（文件 / 邮箱 / 日历 / 浏览器 / 终端）
```

要点：

- **网关是一组诚实命名的工具。** 我们向 agent 提供的工具名如实反映处置：`files.stage_edit`（暂存编辑）、`email.create_draft`（创建草稿）、`email.request_send`（请求发送——注意是 request）。没有一个工具叫 `email.send` 却偷偷不发。
- **策略在你手里。** 按 agent、按工具、按目标（目录/收件人/域名）配置；出厂默认偏保守（三类动作一律批准制）。
- **批准仪式绑定所见。** 批准卡内容的规范化摘要进入记录；你批准的是这张卡，记录里也是这张卡。
- **无云依赖。** 网关、策略、历史全部本地。模型辅助的辅助性功能（如把一夜的动作总结成人话）优先使用端侧模型（见 §6）。

### 5.1 它不是什么

- 不是杀毒软件，不是入侵检测——它不扫描、不拦截未接入的进程。
- 不是另一个 agent——它不替你决策，不自动"纠正"你的 agent。
- 不是合规监控——它是给你自己用的，不是给别人看你的。

## 6. Apple 集成

> 本节所有外部论断的来源与核验状态见 [FEASIBILITY.md](FEASIBILITY.md)。结论先行：**四块 Apple 底座（端侧模型、隔离原语、Touch ID+SE、公证分发）全部可用、全部无需 Apple 个案批准**——这正是"深度借力 Apple 底层资产、不自研底层"路线成立的原因。

### 6.1 端侧 Foundation Models：免费的本地模型选项

Apple 的 Foundation Models 框架自 macOS 26（2025 年秋）起对第三方应用开放：端侧运行、离线可用、推理免费，基础使用**不需要任何 entitlement、waitlist 或 Apple 审批**（仅自训 LoRA adapter 需要专门 entitlement——我们不用 adapter）。所以回答一个直接的问题：接入它不是"第一批尝鲜"，而是采用一个自 2025 年秋 GA 约 9 个月、在 WWDC 2026 又升级了一代的成熟框架——这是好消息。

我们的用法刻意克制：**只做本地摘要与标注，永不替用户决策。**

- 把一夜的动作历史折叠成几句人话（"整理了 83 封邮件，3 封回复草稿等你批准"）；
- 用 guided generation（`@Generable` 类型化输出）把动作打入固定 schema 的标签——约束解码把输出收敛到声明的 schema（其"永不越界"的精确保证程度待真机实证，见 FEASIBILITY §III-6；网关对模型输出始终做确定性校验）；
- **模型产出的摘要与标签仅用于呈现，永不进入策略引擎与三类分类路径**——动作类别只由声明式的工具签名决定（§5）；
- 2025 代端侧模型 4,096 token 的上下文窗口（第三代窗口待确认）决定了喂给它的是逐段动作摘要，不是原始转录——这个限制恰好与"本地、轻量、辅助"的定位一致。

WWDC 2026 新增的更大端侧模型（AFM 3 Core Advanced）、图像输入与 `LanguageModel` 协议（可换第三方模型）是后续选项；新的免费 Private Cloud Compute 档**仅限 App Store 小企业计划成员**，对 Developer ID 分发的应用不可用——无妨，**纯端侧本来就是本产品唯一符合操守的选择**。模型可用性必须运行时检查（设备不支持或 Apple Intelligence 未开启时优雅降级为模板句——我们的句子系统本就是确定性模板，模型只是锦上添花）。

### 6.2 隔离工作区：诚实的选项排序

"agent 在副本里工作、你批准 diff、我们落到真实目录"有三条实现路径，按诚实程度与可达性排序：

1. **用户态暂存工作区（v1 默认）**：副本目录 + 版本化暂存（git 语义）。零 entitlement、零特殊 OS 版本要求、完全用户态——是唯一今天就能对所有用户成立的方案。
2. **Apple Containerization（可选的执行隔离档）**：Apple 官方的 `container`/Containerization 于 2026-06-09 发布 1.0，是可被第三方 Swift 应用内嵌的库；WWDC26 的 container machine 自动共享宿主目录——注意该共享是**实时读写**，用于隔离时必须指向副本目录而非真实目录。它隔离的是 **Linux 虚拟机**——适合把 shell/构建类工作放进去，不是 macOS 原生进程的沙箱。我们将其作为"高危执行档"的选项，如实标注其形态。
3. **不取的路径，如实说明**：App Sandbox 只能约束**我们自己**的进程，不能给别的 agent 上沙箱；APFS 快照的创建是受限 entitlement（Apple 几乎只发给备份厂商）、回滚是私有 entitlement——所以**我们不承诺"系统级快照还原"**，还原点由暂存工作区自己的版本化提供；`sandbox-exec` 已弃用，不做地基。

### 6.3 Touch ID + Secure Enclave：批准仪式

批准仪式的密码学形态：**一次批准 = 一次生物识别门控的签名。**

- Secure Enclave 内驻留一把 P-256 签名私钥，访问控制设为 `.privateKeyUsage + .biometryCurrentSet`：每次签名都要求一次新鲜的 Touch ID 手势（重用时长保持默认 0），私钥永不离开 SE；
- 指纹重新录入会自动作废这把钥匙——这是特性不是缺陷：换了手指，就该重新建立信任；
- 菜单栏应用是 GUI agent，可以正常弹 Touch ID（批准仪式发生在应用里，不在后台 daemon 里——daemon 没有呈现批准卡的资格，这也是架构边界）。

**诚实边界，写进产品而不是藏起来**：Secure Enclave 签的是字节，它不能证明你**看到了什么**。所以批准卡内容（agent 名、动作、目标、参数摘要、风险类别）的规范化哈希由应用计算并**纳入被签名的负载**——签名绑定的是"这张卡"，回放时验的也是"这张卡"。macOS 27 起 App Attest 登陆 Mac 并在 attestation 证书里携带 SE 访问控制条件（ACL Blob OID），为未来由第三方审计方/验证服务确认"这把钥匙确为生物识别门控"提供升级路径（attestation 需经 Apple 证明链验证，与本地优先形态的衔接在采用时另行设计）。

### 6.4 分发：Developer ID 公证的菜单栏应用

- 路径成熟：Apple Developer Program（99 美元/年）→ Developer ID 证书 → Hardened Runtime → `notarytool` 公证 → 装订。自 macOS Sequoia 起未公证软件已没有右键绕行，**正经公证是唯一体面的分发方式**——我们本来也只打算走这条路。
- 公证政策不禁止本地 daemon / 本地服务器形态；Hardened Runtime 禁止注入其他进程——而**不注入**正是本产品的设计原则，两者天然相容。
- 后台项（daemon）经 SMAppService 注册时，系统会向用户显示"已添加后台项"通知并要求批准——这种 OS 级的可见性与我们的同意哲学同向。

### 6.5 为什么是 MCP 网关，而不是 OS 级监控

MCP 已是多厂商开放标准（LF Projects 治理；OpenAI SDK 一方支持；规范 2025-11-25 版现行）。最关键的一句话来自规范本身：MCP 规范**要求** host"在调用任何工具前取得用户的明确同意"，同时声明**协议层无法强制执行**这一点。这个"应当如此但无人执行"的缝隙，就是本产品的位置。

接入方式的完整选项与我们的取舍：

| 路径 | 需要什么 | 我们的取舍 |
|---|---|---|
| **MCP 网关**（agent 配置指向我们的工具） | 零 Apple 许可，纯用户态进程 | **采用**——与操守完全一致：接入即同意 |
| **CLI 包装**（在受监督环境中启动 agent） | 零 Apple 许可（Anthropic 自家的 sandbox-runtime 即此路径的一方验证） | **采用**为第二接入方式 |
| Endpoint Security 框架 | 受限 entitlement，需向 Apple 个案申请、无公开标准，惯例发给安全厂商 | **不取**：对消费级应用不现实，且全局监控形态与"只治理自愿接入"操守相悖 |
| Network Extension 内容过滤 | entitlement 自助 + 系统扩展用户批准 | **不取**（v1）：网络层看不懂动作语义，且形态偏监控 |
| Accessibility / 屏幕观察 | TCC（系统隐私权限）授权 | **不取**：近乎暗中监视，原则排除 |

Apple 自己也在向同一哲学收敛：WWDC26 的 App Intents 加入了基于风险的动作确认机制，Siri 对有后果的动作弹出逐项确认。这验证了"逐动作同意"的方向，但 Apple 没有提供（也未宣布）**跨 agent 的中立批准台账**与**可导出的本地审计历史**——OS 管 Apple 自己的动作面，第三方 agent 的统一治理层仍是空位。

### 6.6 给用户的一句实话

以上每一项能力（端侧模型、容器、SE 签名、公证）都只需要标准开发者资格，**没有任何一项依赖 Apple 的特批**。需要真机实证后才写进承诺的细节（SE 钥匙首启体验、2026 代端侧 API 的 Developer ID 可用性、macOS 27 公证回归等，共 8 项）已列入 [FEASIBILITY.md](FEASIBILITY.md) Part III 待实证清单。

## 7. 覆盖边界：我们如实告诉你网关看不见什么

先把总原则说死：**自愿接入的网关是同意与可见性层，不是围堵边界。** 一个拥有通用 shell / 代码执行能力的 agent，可以直接调用任何 API、驱动无头浏览器——完全绕过 MCP（AWS 安全团队对此有同样的工程结论）。"全覆盖"在这一产品形态下技术上不成立，任何此类宣称都经不起检验。Turing 的承诺只有一句：**凡经由 Turing 通道的动作，可授权、可记录、可回放、可撤销（如果本性可撤销）；通道之外的动作，我们如实告诉你它在通道之外，并教你如何用 agent 自身的配置把边界收窄。**

另有两条**横切边界**，对所有集成一体适用、必须先说：

- **agent 与其模型供应商之间的流量对网关一概不可见**——包括经网关读到的内容被组装进提示送往模型。这不是某家 agent 的缺口，是网关形态本身的边界。
- **网关不解决 prompt injection（提示注入）本身。** 它做的是：即使你的 agent 被不可信内容注入，有后果的动作在执行前仍须经你批准——且仅限经由通道的动作。注入问题的根除不在任何网关的能力范围内（各 agent 官方文档亦如此自述）。

以下为 2026-06 各 agent 的真实接入面与边界（细节与来源见 [FEASIBILITY.md](FEASIBILITY.md)；各 agent 周更级演进，集成文档将随版本滚动更新）：

### 7.1 OpenClaw —— 接入面最厚的首发目标

**接入机制（三层，由薄到厚）**：① 注册为它的 MCP server（`openclaw mcp add`，stdio/HTTP——注意这是 2026 春才落地的较新能力）；② `tools.allow`/`tools.deny` + profiles 可按组关停内置工具（runtime/fs/web/ui/messaging），实现"关内置、只走网关"姿态；③ 最厚一层：**`before_tool_call` 插件 hook**——OpenClaw 文档化的进程内策略闸，文档示例与先例覆盖 exec、message、browser、MCP 等工具类，可改写参数、拦截、或要求批准（完整覆盖矩阵待实测，见 FEASIBILITY §III-3）；另有原生 exec 审批（`tools.exec.mode=ask`）与可选沙箱配置可叠加。已有先例（knostic/openclaw-shield 安全插件）走的就是 hook 路径。

**纯 MCP 网关之外（主要项，完整清单见该集成的边界卡 §7.5）**：内置 exec/process、文件读写编辑、web_search/web_fetch、browser（属 ui 组）、频道发信（message——**它发 WhatsApp/Telegram 消息不经过任何 MCP**）、cron 派发、以及 skills 自带的代码。→ 所以对 OpenClaw 我们默认推荐 **hook + MCP 双层接入**，纯 MCP 模式下的边界在接入向导里逐条展示。

### 7.2 Hermes Agent —— MCP 原生但审批面窄

**接入机制**：`~/.hermes/config.yaml` 的 `mcp_servers`（stdio/HTTP+OAuth，核心循环原生支持，per-server include/exclude）；内置工具可按 toolset 关停（`--toolsets` / `/tools disable`）。

**边界（如实）**：terminal/process（六种执行后端）、`execute_code`（Python 沙箱，**可编程调用其他工具**）、文件工具、browser、**computer_use（macOS 桌面控制）**、`send_message`（各频道回信）、cron 派发与自建技能全部原生执行，不经 MCP；它自己的审批系统**只盖危险 shell 命令**（manual/smart/off），文件写、浏览器、发信都不在审批面内；且在 docker/singularity 等容器后端下危险命令审批被**显式跳过**（文档原话：容器即边界）；per-tool 审批策略与可插拔审批 hook 目前是它的 open feature request。Hermes 尚不能作为 MCP server 反向接入其他客户端。

### 7.3 Claude Code —— hooks 强，但有两个必须写明的缺口

**接入机制**：hooks 系统（PreToolUse 对内置工具逐调用前置裁决：allow/deny/ask/defer + `updatedInput` 改写；handler 可为外部命令 / 本地 HTTP / MCP 工具）+ MCP server 配置 + permissions 规则；macOS 管理式设置（managed settings）可让以上策略防篡改。

**边界（如实，含我们复核出的硬事实）**：① **PreToolUse 的 deny 对 MCP 工具调用不强制执行**——官方 issue 已关闭为 not-planned；对允许列表内的 Edit 也有 deny 失效的已记录案例。所以 Claude Code 的强治理面是内置工具，MCP 工具面要靠 MCP 配置本身收窄；② 云会话（claude.ai/code）跑在 Anthropic 托管 VM 上，**本机 hooks/设置不随行**；③ 模型 API 流量不是工具调用，hook 不可见；④ 非管理式部署下 hooks 配置本身可被修改（`disableAllHooks`）——网关是自愿的，这正是本产品的哲学，但要写明它不防被关。

### 7.4 Codex —— 新生 hooks + 官方自述不完整

**接入机制**：Codex CLI（截至 0.139.0，2026-06-09）已有一等 hooks（`hooks.json` / `config.toml` 的 PreToolUse 等事件；引入版本未锁定，为 2026 年内新生能力）+ MCP（per-server `enabled_tools`/`disabled_tools`）+ `approval_policy`/`sandbox_mode` + Starlark 规则引擎。

**边界（如实）**：OpenAI 自己的文档写明 PreToolUse 拦截**尚不完整**（部分 shell 调用、`unified_exec` 不在拦截面内）；`web_search` 不可被 hook 拦截（但可配置为 disabled/cached）；**Codex Cloud 任务完全在本机网关之外**（OpenAI 托管环境，按云端 per-environment 设置管网络）。

### 7.5 统一披露格式

每个集成在产品内有一页"边界卡"：**经由通道的动作面 / 通道之外的动作面 / 如何收窄 / 本卡对应的 agent 版本与核验日期**。边界卡内容就是本节内容的活文档化——版本一变，边界卡重核。

## 8. 我们服务的生态

### 8.1 OpenClaw：现象与教训并存

OpenClaw（MIT，Peter Steinberger 创建，2025-11 至 2026-01 经历 Warelay→CLAWDIS→Clawdbot→Moltbot→OpenClaw 的连环更名）是个人 agent 时代的引爆点：本地优先 Gateway + 内嵌极简编码 agent（Pi，核心仅 Read/Write/Edit/Bash 四工具）+ 20+ 聊天频道 + SKILL.md 技能生态（ClawHub）。它的安全文档异常坦诚：单一受信操作者模型、默认 host 执行不弹批准、"prompt injection 未解决"是原文。

2026 上半年它的生态付出了真实代价（均为厂商署名、可溯源的公开记录，详见 FEASIBILITY）：ClawHub 上 341 个恶意技能（其中 335 个构成投递 Atomic Stealer 的 ClawHavoc 攻击波）、Snyk ToxicSkills 扫描（3,984 技能中 36% 含缺陷、76 个人工确认恶意载荷）、4 万+ 暴露实例的互联网扫描（暴露主因是操作者错误配置——**当前官方默认是 loopback+鉴权**，我们不重复"默认裸奔"的讹传）、ClawHub 排名操纵漏洞、若干 CVE（含沙箱逃逸链）。OpenClaw 每次都有响应（VirusTotal 全量扫描、举报自动隐藏、审计命令、可选沙箱）——**这不是"OpenClaw 烂"的故事，而是"任何拿到 host 权限的 agent 生态都会经历"的故事**。这正是治理层的需求证据。

### 8.2 Hermes Agent：自我改进 + 自带安全层

Hermes Agent（Nous Research，MIT，2026-02 公开发布，GitHub API 2026-06-11 读数 ~19 万 star）以自我改进循环（自动创建技能、跨会话记忆）、六种执行后端、约 20 个聊天网关、provider 无关路由为特征。它**有**自己的安全层（危险命令审批三模式、硬黑名单、DM 配对、MCP 子进程环境变量白名单）——我们如实承认并赞赏。差异在于：它的审批只盖危险 shell；Turing 提供的是**跨 agent 的统一动作分类、Touch ID 仪式、用户拥有的回执历史**——互补，不替代。

### 8.3 需求证据与竞品诚实

风险不是想象：lethal trifecta 框架（私有数据 × 不可信内容 × 对外通信，Simon Willison 2025-06）、GitHub MCP 注入泄私库（2025-05）、首个野生恶意 MCP server（postmark-mcp 静默密送全部邮件，2025-09）、OWASP Agentic 应用 Top 10（2025-12）。MCP 本身已是 Linux Foundation 治理的多厂商标准（2025-12 捐赠时 >10,000 个公开 server）——攻击面是主流的，不是边缘的。

竞品如实说：企业侧"MCP 防火墙/网关"已经拥挤（Preloop、agentgateway、Lasso、mcp-firewall、Pipelock 等，YAML/CEL 策略、控制面形态）；消费侧最接近的是 Agent Secret（macOS 本地、Touch ID 门控——但只管**凭证**的取用批准）。**跨 agent、消费级、动作级 Touch ID 仪式、本地可回放审计**这个组合目前没有占位者——这是空隙，不是空想（规范层面的法源见 §6.5：MCP 规范要求同意、但协议无法强制执行）。另须如实说明：上述事件中 postmark-mcp 这类"agent 直连恶意工具"的攻击面，恰好属于 §7 所述任何自愿网关的通道之外——我们引它们证明的是问题类别的真实性，不是本产品对它们的覆盖。

## 9. 路线图（草案）

1. **v0.1 网关内核**：MCP 网关 + 三类分类 + 策略引擎 + 黑匣子 + 菜单栏可见性（复用既有 Attention/Glance 设计语言）。过渡形态注明：v0.1 期间三类动作以应用内明确批准（非生物识别）门控，**draft-by-default 自第一版即生效**；v0.2 升级为 Touch ID + SE 签名。
2. **v0.2 批准仪式**：Touch ID + SE 签名批准卡 + 所见绑定。
3. **v0.3 隔离工作区**：一类动作先行区 + diff 批准 + 还原点。
4. **v0.4 首批集成**（按调研后的接入面厚度排序）：**OpenClaw**（hook+MCP+exec 审批三层，接入面最厚）→ **Hermes**（MCP 原生+toolset 关停）→ **Claude Code**（hooks+MCP，边界卡写明 MCP-deny 缺口与云会话例外）→ **Codex**（hooks 新生+官方自述不完整，experimental 档）。每个集成发布时附带 §7.5 边界卡。

## 10. 结语

agent 会越来越能干，这是好事。我们要守住的只有一件事：**能干不等于免检。** 你的电脑、你的账户、你的生活，批准权在你手里——而且批准过什么、基于什么批准的，永远查得到。

这就是 Turing Agentic OS：不是更聪明的 AI，而是让你敢用聪明 AI 的那层保护。
